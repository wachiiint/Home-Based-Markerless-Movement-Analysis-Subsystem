"""Operator CLIs for the calibration workflow.

These are thin wrappers over ``app.services.calibration`` so the metric-3D path
can be exercised with a real printed board before any of it is wrapped in an
HTTP endpoint. Run them with ``python -m app.tools.<name>``.
"""
